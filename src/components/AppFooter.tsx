export function AppFooter() {
  return (
    <footer className="mt-auto border-t border-sand py-8">
      <p className="max-w-prose text-sm text-muted">
        Not veterinary advice. Trait scores are typical for the breed, not a
        promise about an individual animal.
      </p>
    </footer>
  );
}
